export enum Themes {
    DEFAULT = 'DEFAULT',
    DARK = 'DARK',
}

export enum Roles {
    Anonymous = 'Anonymous',
    User = 'User',
    Parent = 'Parent',
    Teacher = 'Teacher',
    Admin = 'Admin',
    SuperAdmin = 'SuperAdmin',
}

export enum ProjectPageStatus {
    Shared = 'Открытый',
    Private = 'Приватный',
    Banned = 'Забанен'
}